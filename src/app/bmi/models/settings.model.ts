export interface UserSettings {
  user_id: string;
  height: 0;
  age: number;
  cookieAccepted: boolean;
  email: string;
}

type Theme = 'light' | 'dark';
type Gender = 'female' | 'male';
